workflows:
  android-workflow:
    name: Android APK Build

    environment:
      java: 17

    scripts:
      - name: Build Debug APK
        script: |
          chmod +x gradlew
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk